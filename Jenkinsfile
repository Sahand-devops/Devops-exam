pipeline {
    agent any

    stages {
        stage('Test') {
            steps {
                sh 'pwd'
                sh 'ls -la'
                sh 'terraform init'
            }
        }
    }
}
